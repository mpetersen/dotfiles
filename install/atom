brew cask install --appdir=/Applications atom

apm upgrade --no-confirm

packages=(
    atom-beautify
    go-plus
    graphviz-preview
    highlight-selected
    language-plantuml
    markdown-format
    plantuml-viewer
)

for package in "${packages[@]}"; do
    apm install $package
done
